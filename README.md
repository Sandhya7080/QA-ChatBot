# QA ChatBot using Watsonx LLM

This project is a chatbot built using IBM Watsonx's LLM (`mixtral-8x7b-instruct-v01`).

## How to Run

1. Create virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate
